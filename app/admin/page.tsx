import AdminClient from './AdminClient';

export const metadata = { title: 'Control Room — Eugine Micah', robots: { index: false, follow: false } };

export default function AdminPage() {
  return <AdminClient />;
}
